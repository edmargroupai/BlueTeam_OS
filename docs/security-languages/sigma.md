# Sigma

YAML rules compile to Python `DetectionRule` objects. Only `selection` / `selection and not filter` is supported in this compiler.

Community rules are not auto-promoted. The Office→PowerShell rule is the first production-tested compiled rule and is asserted in Blue Range.

# SQL

Why: first-class hunting over stored telemetry.

Allowed: registered, parameterized SELECT hunts with MITRE metadata and fixtures.

Prohibited: caller-supplied SQL, multiple statements, INSERT/UPDATE/DELETE/DROP.

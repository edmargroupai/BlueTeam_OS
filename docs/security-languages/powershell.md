# PowerShell

Why: Windows, AD, Entra, M365 investigation.

Allowed: versioned, dry-run, JSON, least-privilege collectors invoked by the broker.

Prohibited: Invoke-Expression, free-form command strings, LLM-direct execution.

Scripts live under `security-languages/powershell` and are static-analysed in CI.

# Rego / OPA

Governs execution authority. Default is DENY. Tier-2 and production impact require approval. AI action types are denied. Python evaluates the published subset; `.rego` files are the human-readable source of truth.

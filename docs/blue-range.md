# Blue Range

Isolated synthetic scenarios under `blue_range/scenarios`. Each fixture declares expected events, detections, ATT&CK IDs, and a latency budget. The runner executes the detection registry and fails CI on assertion errors. No external offensive targets.

# AI gateway

Feature code submits a task to `services/ai-gateway/app/gateway.py`. Detect/correlate/score/contain always return `deterministic_only`. With `BTOS_AI_ENABLED=false` every other task does the same. No provider SDK is imported.
